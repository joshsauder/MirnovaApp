query CourseInfo($user:String!) {
  Courses{
    name
    questionCount
  }
  Completions(user: $user){
    course
    completed
    points
    numberOfTries
    average
  }
}

query Course($name:String!) {
    Course(name: $name){
        questions{
            question
            answer
            image
        }
    }
}
